This pattern came into use, if there is code duplication at sibling level.
If we focus we have code duplication in 'before' package in drive method.
Which we have removed via introducing a new package named as strategy.
If two sibling classes need some same code and that code is not available in parent class, then we should
use strategy design pattern.